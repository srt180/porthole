[project]
name = "porthole"
version = "0.1.2"
description = "A terminal UI for inspecting local listening TCP/UDP ports."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
